===============================================================
==========INSTRUCTIONS TO COMPILE THE SMALLSH PROGRAM==========
===============================================================

1. To compile the program, type this into
   the command line:

      $ gcc --std=gnu99 smallsh.c -o smallsh
      
   This will create an executable file called "smallsh".

2. To run the program, type this into the command line:

      $ smallsh